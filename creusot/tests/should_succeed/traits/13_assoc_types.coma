
module C13AssocTypes_Impl0_Model
  type t
  type model_ty0
  predicate invariant1 (self : model_ty0)
  let rec invariant1 (self:model_ty0) (return'  (ret:bool))= any 
    [ return' (result:bool)-> {result = invariant1 self} (! return' {result}) ]
    
  predicate inv1 (_x : model_ty0)
  let rec inv1 (_x:model_ty0) (return'  (ret:bool))= any 
    [ return' (result:bool)-> {result = inv1 _x} (! return' {result}) ]
    
  axiom inv1 : forall x : model_ty0 . inv1 x = true
  predicate invariant0 (self : t)
  let rec invariant0 (self:t) (return'  (ret:bool))= any 
    [ return' (result:bool)-> {result = invariant0 self} (! return' {result}) ]
    
  predicate inv0 (_x : t)
  let rec inv0 (_x:t) (return'  (ret:bool))= any [ return' (result:bool)-> {result = inv0 _x} (! return' {result}) ] 
  axiom inv0 : forall x : t . inv0 x = true
  use prelude.Borrow
  use prelude.Intrinsic
  predicate resolve0 (self : t)
  let rec resolve0 (self:t) (return'  (ret:bool))= any 
    [ return' (result:bool)-> {result = resolve0 self} (! return' {result}) ]
    
  let rec model (self:t) (return'  (ret:model_ty0))= {[#"../13_assoc_types.rs" 13 13 13 17] inv0 self}
    (! bb0 
    [ bb1 = return' {_0} 
    | bb0 = {[@expl:type invariant] inv0 self}
      -{resolve0 self}- model {self} (fun (_ret':model_ty0) -> [ &_0 <- _ret' ] bb1)  ]
    ) [ return' (result:model_ty0)-> {[#"../13_assoc_types.rs" 13 22 13 35] inv1 result} (! return' {_0}) ] 
    [ & _0 : model_ty0 = any_l () : model_ty0 | & self : t = self ]
    
end
module C13AssocTypes_Impl0
  type t
  type model_ty0
  predicate invariant1 (self : model_ty0)
  let rec invariant1 (self:model_ty0) (return'  (ret:bool))= any 
    [ return' (result:bool)-> {result = invariant1 self} (! return' {result}) ]
    
  predicate inv1 (_x : model_ty0)
  let rec inv1 (_x:model_ty0) (return'  (ret:bool))= any 
    [ return' (result:bool)-> {result = inv1 _x} (! return' {result}) ]
    
  axiom inv1 : forall x : model_ty0 . inv1 x = true
  predicate invariant0 (self : t)
  let rec invariant0 (self:t) (return'  (ret:bool))= any 
    [ return' (result:bool)-> {result = invariant0 self} (! return' {result}) ]
    
  predicate inv0 (_x : t)
  let rec inv0 (_x:t) (return'  (ret:bool))= any [ return' (result:bool)-> {result = inv0 _x} (! return' {result}) ] 
  axiom inv0 : forall x : t . inv0 x = true
  use prelude.Borrow
  goal model_refn : [#"../13_assoc_types.rs" 13 4 13 35] forall self : t . inv0 self
   -> inv0 self /\ (forall result : model_ty0 . inv1 result  -> inv1 result)
end
