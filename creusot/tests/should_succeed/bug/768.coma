
module T_768__A
  use prelude.prelude.UIntSize
  
  use prelude.prelude.Int
  
  type t_A  =
    | C_A usize usize
  
  function any_l (_ : 'b) : 'a
  
  let rec t_A (input:t_A) (ret  (l:usize) (r:usize))= any
    [ good (l:usize) (r:usize)-> {C_A l r = input} (! ret {l} {r}) ]
    
  
  function t_A__r (self : t_A) : usize =
    match self with
      | C_A _ a -> a
      end
end
