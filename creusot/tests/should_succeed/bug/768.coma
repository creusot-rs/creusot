
module C768_A_Type
  use prelude.UIntSize
  use prelude.Int
  type t_a  =
    | C_A usize usize
    
  function any_l (_ : 'b) : 'a
  function a_r (self : t_a) : usize =
    match self with
      | C_A _ a -> a
      end
end
