[package]
name = "why3tests"
version = "0.1.0"
edition = "2018"

# This package is a placeholder used to run the why3 tests without requiring
[dev-dependencies]
glob = "*"
assert_cmd = "1.0"
termcolor = "1.1"

[[test]]
test = false
name = "why3"
harness = false
