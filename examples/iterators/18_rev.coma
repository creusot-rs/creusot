module M_common__trait_ExactSizeIterator__is_empty
  use creusot.int.UInt64
  use seq.Seq
  use creusot.prelude.MutBorrow
  use creusot.prelude.Any
  use int.Int
  
  type t_Self
  
  predicate inv_Self (_1: t_Self)
  
  predicate invariant_ref_Self [@inline:trivial] (self: t_Self) = inv_Self self
  
  meta "rewrite_def" predicate invariant_ref_Self
  
  predicate inv_ref_Self [@inline:trivial] (_1: t_Self) = invariant_ref_Self _1
  
  meta "rewrite_def" predicate inv_ref_Self
  
  predicate precondition_len (self: ()) (args: t_Self)
  
  axiom precondition_fndef: forall args: t_Self [precondition_len () args]. (let self = args in inv_ref_Self self)
      -> precondition_len () args
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  type t_Item
  
  predicate produces_Self (self: t_Self) (visited: Seq.seq t_Item) (o: t_Self)
  
  function produces_trans_Self (a: t_Self) (ab: Seq.seq t_Item) (b: t_Self) (bc: Seq.seq t_Item) (c: t_Self) : ()
  
  axiom produces_trans_Self_spec:
    forall a: t_Self, ab: Seq.seq t_Item, b: t_Self, bc: Seq.seq t_Item, c: t_Self. produces_Self a ab b
      -> produces_Self b bc c -> produces_Self a (Seq.(++) ab bc) c
  
  function produces_refl_Self (self: t_Self) : ()
  
  axiom produces_refl_Self_spec: forall self: t_Self. produces_Self self (Seq.empty: Seq.seq t_Item) self
  
  predicate completed_Self (self: MutBorrow.t t_Self)
  
  predicate postcondition_size_hint (self: ()) (args: t_Self) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_Self, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_Self. produces_Self self s i.current
            /\ completed_Self i -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_Self. produces_Self self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate precondition_size_hint (self: ()) (args: t_Self)
  
  axiom precondition_fndef'0:
    forall args: t_Self [precondition_size_hint () args]. (let self = args in inv_ref_Self self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_Self) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_Self (self: t_Self) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate postcondition_len (self: ()) (args: t_Self) (result: UInt64.t)
  
  axiom postcondition_fndef'0:
    forall args: t_Self, res: UInt64.t [postcondition_len () args res]. postcondition_len () args res
      -> (let self = args in postcondition_size_hint () self { f0 = res; f1 = Some res })
  
  predicate postcondition_once_len (self: ()) (args: t_Self) (result: UInt64.t) = postcondition_len self args result
  
  meta "rewrite_def" predicate postcondition_once_len
  
  let len_Self (self: t_Self) (return (x: UInt64.t)) = {[@stop_split] [@expl:len requires] precondition_len () self}
    any
    [ return (result: UInt64.t) -> {[@stop_split] [@expl:len ensures] postcondition_once_len () self result}
      (! return {result}) ]
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let is_empty_Self (self: t_Self) (return (x: bool)) =
  {[@stop_split] [@expl:is_empty 'self' type invariant] inv_ref_Self self}
    (! bb0
    [ bb0 = s0
      [ s0 = len_Self {self} (fun (_x: UInt64.t) -> [ &_4 <- _x ] s1)
      | s1 = [ &_ret <- _4 = (0: UInt64.t) ] s2
      | s2 = return {_ret} ] ] [ & _ret: bool = Any.any_l () | & self: t_Self = self | & _4: UInt64.t = Any.any_l () ])
    [ return (result: bool) ->
    {[@stop_split] [@expl:is_empty ensures] exists l: UInt64.t. postcondition_size_hint () self { f0 = l; f1 = Some l }
        /\ result = (l = (0: UInt64.t))}
      (! return {result}) ]
end
module M_common__trait_ExactSizeIterator__len
  use creusot.int.UInt64
  use seq.Seq
  use creusot.prelude.MutBorrow
  use creusot.prelude.Any
  use int.Int
  
  type t_Self
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  type t_Item
  
  predicate produces_Self (self: t_Self) (visited: Seq.seq t_Item) (o: t_Self)
  
  function produces_trans_Self (a: t_Self) (ab: Seq.seq t_Item) (b: t_Self) (bc: Seq.seq t_Item) (c: t_Self) : ()
  
  axiom produces_trans_Self_spec:
    forall a: t_Self, ab: Seq.seq t_Item, b: t_Self, bc: Seq.seq t_Item, c: t_Self. produces_Self a ab b
      -> produces_Self b bc c -> produces_Self a (Seq.(++) ab bc) c
  
  function produces_refl_Self (self: t_Self) : ()
  
  axiom produces_refl_Self_spec: forall self: t_Self. produces_Self self (Seq.empty: Seq.seq t_Item) self
  
  predicate completed_Self (self: MutBorrow.t t_Self)
  
  predicate postcondition_size_hint (self: ()) (args: t_Self) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_Self, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_Self. produces_Self self s i.current
            /\ completed_Self i -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_Self. produces_Self self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate inv_Self (_1: t_Self)
  
  predicate invariant_ref_Self [@inline:trivial] (self: t_Self) = inv_Self self
  
  meta "rewrite_def" predicate invariant_ref_Self
  
  predicate inv_ref_Self [@inline:trivial] (_1: t_Self) = invariant_ref_Self _1
  
  meta "rewrite_def" predicate inv_ref_Self
  
  predicate precondition_size_hint (self: ()) (args: t_Self)
  
  axiom precondition_fndef: forall args: t_Self [precondition_size_hint () args]. (let self = args in inv_ref_Self self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_Self) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_Self (self: t_Self) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  function size_hint_exact_Self (self: t_Self) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_exact_Self_spec: forall self: t_Self, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> r.f1 = Some (r.f0)
  
  type tup2_ref_Option_usize_ref_Option_usize = { f0'0: t_Option_usize; f1'0: t_Option_usize }
  
  type t_Option_Int = None'0 | Some'0 int
  
  function deep_model_usize [@inline:trivial] (self: UInt64.t) : int = UInt64.t'int self
  
  meta "rewrite_def" function deep_model_usize
  
  function deep_model_Option_usize [@inline:trivial] (self: t_Option_usize) : t_Option_Int = match self with
      | Some t -> Some'0 (deep_model_usize t)
      | None -> None'0
      end
  
  meta "rewrite_def" function deep_model_Option_usize
  
  function deep_model_ref_Option_usize [@inline:trivial] (self: t_Option_usize) : t_Option_Int =
    deep_model_Option_usize self
  
  meta "rewrite_def" function deep_model_ref_Option_usize
  
  let eq_Option_usize (self_: t_Option_usize) (rhs: t_Option_usize) (return (x: bool)) = any
    [ return (result: bool) -> {[@stop_split] [@expl:eq ensures] result
      = (deep_model_ref_Option_usize self_ = deep_model_ref_Option_usize rhs)}
      (! return {result}) ]
  
  type t_AssertKind = Eq | Ne | Match
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let len_Self (self: t_Self) (return (x: UInt64.t)) =
  {[@stop_split] [@expl:len 'self' type invariant] inv_ref_Self self}
    (! bb0
    [ bb0 = s0
      [ s0 = [ &_4 <- () ] s1
      | s1 = size_hint_Self {self} (fun (_x: tup2_usize_Option_usize) -> [ &_8 <- _x ] s2)
      | s2 = [ &lower <- _8.f0 ] s3
      | s3 = [ &upper <- _8.f1 ] s4
      | s4 = [ &_14 <- Some lower ] s5
      | s5 = [ &_11 <- { f0'0 = upper; f1'0 = _14 } ] s6
      | s6 = [ &left_val <- _11.f0'0 ] s7
      | s7 = [ &right_val <- _11.f1'0 ] s8
      | s8 = eq_Option_usize {left_val} {right_val} (fun (_x: bool) -> [ &_18 <- _x ] s9)
      | s9 = any [ br0 -> {_18 = false} (! bb5) | br1 -> {_18} (! bb4) ] ]
    | bb4 = s0 [ s0 = [ &_ret <- lower ] s1 | s1 = return {_ret} ]
    | bb5 = s0
      [ s0 = [ &kind <- Eq ] s1 | s1 = [ &_26 <- left_val ] s2 | s2 = [ &_28 <- right_val ] s3 | s3 = {false} any ] ]
    [ & _ret: UInt64.t = Any.any_l ()
    | & self: t_Self = self
    | & _4: () = Any.any_l ()
    | & lower: UInt64.t = Any.any_l ()
    | & upper: t_Option_usize = Any.any_l ()
    | & _8: tup2_usize_Option_usize = Any.any_l ()
    | & _11: tup2_ref_Option_usize_ref_Option_usize = Any.any_l ()
    | & _14: t_Option_usize = Any.any_l ()
    | & left_val: t_Option_usize = Any.any_l ()
    | & right_val: t_Option_usize = Any.any_l ()
    | & _18: bool = Any.any_l ()
    | & kind: t_AssertKind = Any.any_l ()
    | & _26: t_Option_usize = Any.any_l ()
    | & _28: t_Option_usize = Any.any_l () ])
    [ return (result: UInt64.t) -> {[@stop_split] [@expl:len ensures] postcondition_size_hint () self { f0 = result;
                                                                                                        f1 = Some result }}
      (! return {result}) ]
end
module M_impl_DoubleEndedIterator_for_Rev_I__next_back (* <Rev<I> as common::DoubleEndedIterator> *)
  use creusot.prelude.MutBorrow
  use seq.Seq
  use creusot.prelude.Any
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  predicate inv_I (_1: t_I)
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  type t_Option_Item = None | Some t_Item
  
  predicate invariant_refmut_I [@inline:trivial] (self: MutBorrow.t t_I) = inv_I self.current /\ inv_I self.final
  
  meta "rewrite_def" predicate invariant_refmut_I
  
  predicate inv_refmut_I [@inline:trivial] (_1: MutBorrow.t t_I) = invariant_refmut_I _1
  
  meta "rewrite_def" predicate inv_refmut_I
  
  predicate precondition_next (self: ()) (args: MutBorrow.t t_I)
  
  axiom precondition_fndef:
    forall args: MutBorrow.t t_I [precondition_next () args]. (let self = args in inv_refmut_I self)
      -> precondition_next () args
  
  predicate inv_Item (_1: t_Item)
  
  predicate inv_Option_Item (_1: t_Option_Item)
  
  axiom inv_axiom [@rewrite]: forall x: t_Option_Item [inv_Option_Item x]. inv_Option_Item x
      = match x with
        | None -> true
        | Some f0 -> inv_Item f0
        end
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_next (self: ()) (args: MutBorrow.t t_I) (result: t_Option_Item)
  
  axiom postcondition_fndef:
    forall args: MutBorrow.t t_I, res: t_Option_Item [postcondition_next () args res]. postcondition_next () args res
      -> (let self = args in inv_Option_Item res
      /\ match res with
        | None -> completed_I self
        | Some v -> produces_I self.current (Seq.singleton v) self.final
        end)
  
  predicate postcondition_once_next (self: ()) (args: MutBorrow.t t_I) (result: t_Option_Item) =
    postcondition_next self args result
  
  meta "rewrite_def" predicate postcondition_once_next
  
  let next_I (self: MutBorrow.t t_I) (return (x: t_Option_Item)) =
  {[@stop_split] [@expl:next requires] precondition_next () self}
    any
    [ return (result: t_Option_Item) -> {[@stop_split] [@expl:next ensures] postcondition_once_next () self result}
      (! return {result}) ]
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom'0 [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_refmut_Rev_I [@inline:trivial] (self: MutBorrow.t t_Rev_I) =
    inv_Rev_I self.current /\ inv_Rev_I self.final
  
  meta "rewrite_def" predicate invariant_refmut_Rev_I
  
  predicate inv_refmut_Rev_I [@inline:trivial] (_1: MutBorrow.t t_Rev_I) = invariant_refmut_Rev_I _1
  
  meta "rewrite_def" predicate inv_refmut_Rev_I
  
  predicate resolve_refmut_Rev_I [@inline:trivial] (_1: MutBorrow.t t_Rev_I) = _1.final = _1.current
  
  meta "rewrite_def" predicate resolve_refmut_Rev_I
  
  predicate completed_back_Rev_I (self: MutBorrow.t t_Rev_I) =
    completed_I (MutBorrow.borrow_logic self.current.iter self.final.iter (MutBorrow.inherit_id (MutBorrow.get_id self) 0))
  
  predicate produces_back_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_I self.iter visited o.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let next_back_Rev_I (self: MutBorrow.t t_Rev_I) (return (x: t_Option_Item)) =
  {[@stop_split] [@expl:next_back 'self' type invariant] inv_refmut_Rev_I self}
    (! bb0
    [ bb0 = s0
      [ s0 = MutBorrow.borrow_final <t_I> {self.current.iter} {MutBorrow.inherit_id (MutBorrow.get_id self) 0}
          (fun (_bor: MutBorrow.t t_I) ->
            [ &_4 <- _bor ] -{inv_I _bor.final}-
            [ &self <- { self with current = { iter = _bor.final } } ] s1)
        [ _ck -> (! {[@expl:type invariant] inv_I self.current.iter} any) ]
      | s1 = next_I {_4} (fun (_x: t_Option_Item) -> [ &_ret <- _x ] s2)
      | s2 = s3 [ _ck -> (! {[@expl:type invariant] inv_refmut_Rev_I self} any) ]
      | s3 = -{resolve_refmut_Rev_I self}- s4
      | s4 = return {_ret} ] ]
    [ & _ret: t_Option_Item = Any.any_l ()
    | & self: MutBorrow.t t_Rev_I = self
    | & _4: MutBorrow.t t_I = Any.any_l () ])
    [ return (result: t_Option_Item) ->
    {[@stop_split] [@expl:next_back_Rev_I ensures] ([@stop_split] [@expl:next_back result type invariant] inv_Option_Item result)
      /\ ([@stop_split] [@expl:next_back ensures] match result with
        | None -> completed_back_Rev_I self
        | Some v -> produces_back_Rev_I self.current (Seq.singleton v) self.final
        end)}
      (! return {result}) ]
end
module M_impl_DoubleEndedIterator_for_Rev_I__next_back__refines (* <Rev<I> as common::DoubleEndedIterator> *)
  use creusot.prelude.MutBorrow
  use seq.Seq
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  predicate inv_I (_1: t_I)
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_refmut_Rev_I [@inline:trivial] (self: MutBorrow.t t_Rev_I) =
    inv_Rev_I self.current /\ inv_Rev_I self.final
  
  meta "rewrite_def" predicate invariant_refmut_Rev_I
  
  predicate inv_refmut_Rev_I [@inline:trivial] (_1: MutBorrow.t t_Rev_I) = invariant_refmut_Rev_I _1
  
  meta "rewrite_def" predicate inv_refmut_Rev_I
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  type t_Option_Item = None | Some t_Item
  
  predicate inv_Item (_1: t_Item)
  
  predicate inv_Option_Item (_1: t_Option_Item)
  
  axiom inv_axiom'0 [@rewrite]: forall x: t_Option_Item [inv_Option_Item x]. inv_Option_Item x
      = match x with
        | None -> true
        | Some f0 -> inv_Item f0
        end
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate completed_back_Rev_I (self: MutBorrow.t t_Rev_I) =
    completed_I (MutBorrow.borrow_logic self.current.iter self.final.iter (MutBorrow.inherit_id (MutBorrow.get_id self) 0))
  
  predicate produces_back_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_I self.iter visited o.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines: forall self: MutBorrow.t t_Rev_I. inv_refmut_Rev_I self
      -> inv_refmut_Rev_I self
      /\ (forall result: t_Option_Item. inv_Option_Item result
          /\ match result with
            | None -> completed_back_Rev_I self
            | Some v -> produces_back_Rev_I self.current (Seq.singleton v) self.final
            end
        -> inv_Option_Item result
        /\ match result with
          | None -> completed_back_Rev_I self
          | Some v -> produces_back_Rev_I self.current (Seq.singleton v) self.final
          end)
end
module M_impl_DoubleEndedIterator_for_Rev_I__produces_back_refl (* <Rev<I> as common::DoubleEndedIterator> *)
  use seq.Seq
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_back_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_I self.iter visited o.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  constant self : t_Rev_I
  
  function produces_back_refl_Rev_I (self: t_Rev_I) : ()
  
  goal vc_produces_back_refl_Rev_I:
    [@stop_split] [@expl:produces_back_refl ensures] produces_back_Rev_I self (Seq.empty: Seq.seq t_Item) self
end
module M_impl_DoubleEndedIterator_for_Rev_I__produces_back_refl__refines (* <Rev<I> as common::DoubleEndedIterator> *)
  use seq.Seq
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_back_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_I self.iter visited o.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines: forall self: t_Rev_I. forall result: (). produces_back_Rev_I self (Seq.empty: Seq.seq t_Item) self
        -> produces_back_Rev_I self (Seq.empty: Seq.seq t_Item) self
end
module M_impl_DoubleEndedIterator_for_Rev_I__produces_back_trans (* <Rev<I> as common::DoubleEndedIterator> *)
  use seq.Seq
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_back_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_I self.iter visited o.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  constant a : t_Rev_I
  
  constant ab : Seq.seq t_Item
  
  constant b : t_Rev_I
  
  constant bc : Seq.seq t_Item
  
  constant c : t_Rev_I
  
  function produces_back_trans_Rev_I (a: t_Rev_I) (ab: Seq.seq t_Item) (b: t_Rev_I) (bc: Seq.seq t_Item) (c: t_Rev_I) : ()
  
  goal vc_produces_back_trans_Rev_I: produces_back_Rev_I a ab b
    -> produces_back_Rev_I b bc c
    -> ([@stop_split] [@expl:produces_back_trans ensures] produces_back_Rev_I a (Seq.(++) ab bc) c)
end
module M_impl_DoubleEndedIterator_for_Rev_I__produces_back_trans__refines (* <Rev<I> as common::DoubleEndedIterator> *)
  use seq.Seq
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_back_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_I self.iter visited o.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines:
    forall a: t_Rev_I, ab: Seq.seq t_Item, b: t_Rev_I, bc: Seq.seq t_Item, c: t_Rev_I. produces_back_Rev_I a ab b
      -> produces_back_Rev_I b bc c
      -> produces_back_Rev_I a ab b
      /\ produces_back_Rev_I b bc c
      /\ (forall result: (). produces_back_Rev_I a (Seq.(++) ab bc) c -> produces_back_Rev_I a (Seq.(++) ab bc) c)
end
module M_impl_DoubleEndedIterator_for_Rev_I__size_hint_back_spec (* <Rev<I> as common::DoubleEndedIterator> *)
  use creusot.int.UInt64
  use seq.Seq
  use creusot.prelude.MutBorrow
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate postcondition_size_hint'0 (self: ()) (args: t_Rev_I) (result: tup2_usize_Option_usize) =
    let self'0 = args in postcondition_size_hint () self'0.iter result
  
  meta "rewrite_def" predicate postcondition_size_hint'0
  
  predicate inv_I (_1: t_I)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  function produces_trans_Rev_I (a: t_Rev_I) (ab: Seq.seq t_Item) (b: t_Rev_I) (bc: Seq.seq t_Item) (c: t_Rev_I) : () =
    ()
  
  axiom produces_trans_Rev_I_spec:
    forall a: t_Rev_I, ab: Seq.seq t_Item, b: t_Rev_I, bc: Seq.seq t_Item, c: t_Rev_I. produces_Rev_I a ab b
      -> produces_Rev_I b bc c -> produces_Rev_I a (Seq.(++) ab bc) c
  
  function produces_refl_Rev_I (self: t_Rev_I) : () = ()
  
  axiom produces_refl_Rev_I_spec: forall self: t_Rev_I. produces_Rev_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_ref_Rev_I [@inline:trivial] (self: t_Rev_I) = inv_Rev_I self
  
  meta "rewrite_def" predicate invariant_ref_Rev_I
  
  predicate inv_ref_Rev_I [@inline:trivial] (_1: t_Rev_I) = invariant_ref_Rev_I _1
  
  meta "rewrite_def" predicate inv_ref_Rev_I
  
  let size_hint_Rev_I (self: t_Rev_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint 'self' type invariant] inv_ref_Rev_I self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_size_hint () self.iter result}
      (! return {result}) ]
  
  predicate produces_back_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_I self.iter visited o.iter
  
  predicate completed_back_Rev_I (self: MutBorrow.t t_Rev_I) =
    completed_I (MutBorrow.borrow_logic self.current.iter self.final.iter (MutBorrow.inherit_id (MutBorrow.get_id self) 0))
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  constant self : t_Rev_I
  
  constant r : tup2_usize_Option_usize
  
  function size_hint_back_spec_Rev_I (self: t_Rev_I) (r: tup2_usize_Option_usize) : ()
  
  goal vc_size_hint_back_spec_Rev_I: postcondition_size_hint'0 () self r
    -> ([@stop_split] [@expl:size_hint_back_spec_Rev_I ensures] ([@stop_split] [@expl:size_hint_back_spec ensures #0] forall s: Seq.seq t_Item, i: MutBorrow.t t_Rev_I. produces_back_Rev_I self s i.current
          /\ completed_back_Rev_I i -> UInt64.t'int r.f0 <= Seq.length s)
    /\ ([@stop_split] [@expl:size_hint_back_spec ensures #1] match r.f1 with
      | Some r'0 -> forall s: Seq.seq t_Item, i: t_Rev_I. produces_back_Rev_I self s i
        -> Seq.length s <= UInt64.t'int r'0
      | None -> true
      end))
end
module M_impl_DoubleEndedIterator_for_Rev_I__size_hint_back_spec__refines (* <Rev<I> as common::DoubleEndedIterator> *)
  use creusot.int.UInt64
  use seq.Seq
  use creusot.prelude.MutBorrow
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate postcondition_size_hint'0 (self: ()) (args: t_Rev_I) (result: tup2_usize_Option_usize) =
    let self'0 = args in postcondition_size_hint () self'0.iter result
  
  meta "rewrite_def" predicate postcondition_size_hint'0
  
  predicate inv_I (_1: t_I)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  function produces_trans_Rev_I (a: t_Rev_I) (ab: Seq.seq t_Item) (b: t_Rev_I) (bc: Seq.seq t_Item) (c: t_Rev_I) : () =
    ()
  
  axiom produces_trans_Rev_I_spec:
    forall a: t_Rev_I, ab: Seq.seq t_Item, b: t_Rev_I, bc: Seq.seq t_Item, c: t_Rev_I. produces_Rev_I a ab b
      -> produces_Rev_I b bc c -> produces_Rev_I a (Seq.(++) ab bc) c
  
  function produces_refl_Rev_I (self: t_Rev_I) : () = ()
  
  axiom produces_refl_Rev_I_spec: forall self: t_Rev_I. produces_Rev_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_ref_Rev_I [@inline:trivial] (self: t_Rev_I) = inv_Rev_I self
  
  meta "rewrite_def" predicate invariant_ref_Rev_I
  
  predicate inv_ref_Rev_I [@inline:trivial] (_1: t_Rev_I) = invariant_ref_Rev_I _1
  
  meta "rewrite_def" predicate inv_ref_Rev_I
  
  let size_hint_Rev_I (self: t_Rev_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint 'self' type invariant] inv_ref_Rev_I self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_size_hint () self.iter result}
      (! return {result}) ]
  
  predicate produces_back_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_I self.iter visited o.iter
  
  predicate completed_back_Rev_I (self: MutBorrow.t t_Rev_I) =
    completed_I (MutBorrow.borrow_logic self.current.iter self.final.iter (MutBorrow.inherit_id (MutBorrow.get_id self) 0))
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines: forall self: t_Rev_I, r: tup2_usize_Option_usize. postcondition_size_hint'0 () self r
      -> postcondition_size_hint'0 () self r
      /\ (forall result: (). (forall s: Seq.seq t_Item, i: MutBorrow.t t_Rev_I. produces_back_Rev_I self s i.current
                /\ completed_back_Rev_I i -> UInt64.t'int r.f0 <= Seq.length s)
          /\ match r.f1 with
            | Some r'0 -> forall s: Seq.seq t_Item, i: t_Rev_I. produces_back_Rev_I self s i
              -> Seq.length s <= UInt64.t'int r'0
            | None -> true
            end
        -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_Rev_I. produces_back_Rev_I self s i.current
              /\ completed_back_Rev_I i -> UInt64.t'int r.f0 <= Seq.length s)
        /\ match r.f1 with
          | Some r'0 -> forall s: Seq.seq t_Item, i: t_Rev_I. produces_back_Rev_I self s i
            -> Seq.length s <= UInt64.t'int r'0
          | None -> true
          end)
end
module M_impl_ExactSizeIterator_for_Rev_I__is_empty (* <Rev<I> as common::ExactSizeIterator> *)
  use seq.Seq
  use creusot.int.UInt64
  use creusot.prelude.MutBorrow
  use creusot.prelude.Any
  use int.Int
  
  type t_I
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  type t_Rev_I = { iter: t_I }
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate inv_I (_1: t_I)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  function size_hint_exact_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_exact_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> r.f1 = Some (r.f0)
  
  predicate precondition_is_empty (self: ()) (args: t_I)
  
  axiom precondition_fndef'0: forall args: t_I [precondition_is_empty () args]. (let self = args in inv_ref_I self)
      -> precondition_is_empty () args
  
  predicate postcondition_is_empty (self: ()) (args: t_I) (result: bool)
  
  axiom postcondition_fndef'0:
    forall args: t_I, res: bool [postcondition_is_empty () args res]. postcondition_is_empty () args res
      -> (let self = args in exists l: UInt64.t. postcondition_size_hint () self { f0 = l; f1 = Some l }
        /\ res = (l = (0: UInt64.t)))
  
  predicate postcondition_once_is_empty (self: ()) (args: t_I) (result: bool) = postcondition_is_empty self args result
  
  meta "rewrite_def" predicate postcondition_once_is_empty
  
  let is_empty_I (self: t_I) (return (x: bool)) =
  {[@stop_split] [@expl:is_empty requires] precondition_is_empty () self}
    any
    [ return (result: bool) -> {[@stop_split] [@expl:is_empty ensures] postcondition_once_is_empty () self result}
      (! return {result}) ]
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_ref_Rev_I [@inline:trivial] (self: t_Rev_I) = inv_Rev_I self
  
  meta "rewrite_def" predicate invariant_ref_Rev_I
  
  predicate inv_ref_Rev_I [@inline:trivial] (_1: t_Rev_I) = invariant_ref_Rev_I _1
  
  meta "rewrite_def" predicate inv_ref_Rev_I
  
  predicate postcondition_size_hint'0 (self: ()) (args: t_Rev_I) (result: tup2_usize_Option_usize) =
    let self'0 = args in postcondition_size_hint () self'0.iter result
  
  meta "rewrite_def" predicate postcondition_size_hint'0
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  function produces_trans_Rev_I (a: t_Rev_I) (ab: Seq.seq t_Item) (b: t_Rev_I) (bc: Seq.seq t_Item) (c: t_Rev_I) : () =
    ()
  
  axiom produces_trans_Rev_I_spec:
    forall a: t_Rev_I, ab: Seq.seq t_Item, b: t_Rev_I, bc: Seq.seq t_Item, c: t_Rev_I. produces_Rev_I a ab b
      -> produces_Rev_I b bc c -> produces_Rev_I a (Seq.(++) ab bc) c
  
  function produces_refl_Rev_I (self: t_Rev_I) : () = ()
  
  axiom produces_refl_Rev_I_spec: forall self: t_Rev_I. produces_Rev_I self (Seq.empty: Seq.seq t_Item) self
  
  let size_hint_Rev_I (self: t_Rev_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint 'self' type invariant] inv_ref_Rev_I self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_size_hint () self.iter result}
      (! return {result}) ]
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let is_empty_Rev_I (self: t_Rev_I) (return (x: bool)) =
  {[@stop_split] [@expl:is_empty 'self' type invariant] inv_ref_Rev_I self}
    (! bb0
    [ bb0 = s0
      [ s0 = {[@expl:assertion] forall s: Seq.seq t_Item. Seq.length s = 0 -> s = (Seq.empty: Seq.seq t_Item)} s1
      | s1 = is_empty_I {self.iter} (fun (_x: bool) -> [ &_ret <- _x ] s2)
      | s2 = return {_ret} ] ] [ & _ret: bool = Any.any_l () | & self: t_Rev_I = self ])
    [ return (result: bool) ->
    {[@stop_split] [@expl:is_empty ensures] exists l: UInt64.t. postcondition_size_hint'0 () self { f0 = l;
                                                                                                    f1 = Some l }
        /\ result = (l = (0: UInt64.t))}
      (! return {result}) ]
end
module M_impl_ExactSizeIterator_for_Rev_I__is_empty__refines (* <Rev<I> as common::ExactSizeIterator> *)
  use creusot.int.UInt64
  use seq.Seq
  use creusot.prelude.MutBorrow
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  predicate inv_I (_1: t_I)
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_ref_Rev_I [@inline:trivial] (self: t_Rev_I) = inv_Rev_I self
  
  meta "rewrite_def" predicate invariant_ref_Rev_I
  
  predicate inv_ref_Rev_I [@inline:trivial] (_1: t_Rev_I) = invariant_ref_Rev_I _1
  
  meta "rewrite_def" predicate inv_ref_Rev_I
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate postcondition_size_hint'0 (self: ()) (args: t_Rev_I) (result: tup2_usize_Option_usize) =
    let self'0 = args in postcondition_size_hint () self'0.iter result
  
  meta "rewrite_def" predicate postcondition_size_hint'0
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  function produces_trans_Rev_I (a: t_Rev_I) (ab: Seq.seq t_Item) (b: t_Rev_I) (bc: Seq.seq t_Item) (c: t_Rev_I) : () =
    ()
  
  axiom produces_trans_Rev_I_spec:
    forall a: t_Rev_I, ab: Seq.seq t_Item, b: t_Rev_I, bc: Seq.seq t_Item, c: t_Rev_I. produces_Rev_I a ab b
      -> produces_Rev_I b bc c -> produces_Rev_I a (Seq.(++) ab bc) c
  
  function produces_refl_Rev_I (self: t_Rev_I) : () = ()
  
  axiom produces_refl_Rev_I_spec: forall self: t_Rev_I. produces_Rev_I self (Seq.empty: Seq.seq t_Item) self
  
  let size_hint_Rev_I (self: t_Rev_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint 'self' type invariant] inv_ref_Rev_I self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_size_hint () self.iter result}
      (! return {result}) ]
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines: forall self: t_Rev_I. inv_ref_Rev_I self
      -> inv_ref_Rev_I self
      /\ (forall result: bool. (exists l: UInt64.t. postcondition_size_hint'0 () self { f0 = l; f1 = Some l }
            /\ result = (l = (0: UInt64.t)))
        -> (exists l: UInt64.t. postcondition_size_hint'0 () self { f0 = l; f1 = Some l }
          /\ result = (l = (0: UInt64.t))))
end
module M_impl_ExactSizeIterator_for_Rev_I__len (* <Rev<I> as common::ExactSizeIterator> *)
  use creusot.int.UInt64
  use seq.Seq
  use creusot.prelude.MutBorrow
  use creusot.prelude.Any
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate inv_I (_1: t_I)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  function size_hint_exact_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_exact_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> r.f1 = Some (r.f0)
  
  predicate precondition_len (self: ()) (args: t_I)
  
  axiom precondition_fndef'0: forall args: t_I [precondition_len () args]. (let self = args in inv_ref_I self)
      -> precondition_len () args
  
  predicate postcondition_len (self: ()) (args: t_I) (result: UInt64.t)
  
  axiom postcondition_fndef'0:
    forall args: t_I, res: UInt64.t [postcondition_len () args res]. postcondition_len () args res
      -> (let self = args in postcondition_size_hint () self { f0 = res; f1 = Some res })
  
  predicate postcondition_once_len (self: ()) (args: t_I) (result: UInt64.t) = postcondition_len self args result
  
  meta "rewrite_def" predicate postcondition_once_len
  
  let len_I (self: t_I) (return (x: UInt64.t)) = {[@stop_split] [@expl:len requires] precondition_len () self}
    any
    [ return (result: UInt64.t) -> {[@stop_split] [@expl:len ensures] postcondition_once_len () self result}
      (! return {result}) ]
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_ref_Rev_I [@inline:trivial] (self: t_Rev_I) = inv_Rev_I self
  
  meta "rewrite_def" predicate invariant_ref_Rev_I
  
  predicate inv_ref_Rev_I [@inline:trivial] (_1: t_Rev_I) = invariant_ref_Rev_I _1
  
  meta "rewrite_def" predicate inv_ref_Rev_I
  
  predicate postcondition_size_hint'0 (self: ()) (args: t_Rev_I) (result: tup2_usize_Option_usize) =
    let self'0 = args in postcondition_size_hint () self'0.iter result
  
  meta "rewrite_def" predicate postcondition_size_hint'0
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  function produces_trans_Rev_I (a: t_Rev_I) (ab: Seq.seq t_Item) (b: t_Rev_I) (bc: Seq.seq t_Item) (c: t_Rev_I) : () =
    ()
  
  axiom produces_trans_Rev_I_spec:
    forall a: t_Rev_I, ab: Seq.seq t_Item, b: t_Rev_I, bc: Seq.seq t_Item, c: t_Rev_I. produces_Rev_I a ab b
      -> produces_Rev_I b bc c -> produces_Rev_I a (Seq.(++) ab bc) c
  
  function produces_refl_Rev_I (self: t_Rev_I) : () = ()
  
  axiom produces_refl_Rev_I_spec: forall self: t_Rev_I. produces_Rev_I self (Seq.empty: Seq.seq t_Item) self
  
  let size_hint_Rev_I (self: t_Rev_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint 'self' type invariant] inv_ref_Rev_I self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_size_hint () self.iter result}
      (! return {result}) ]
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let len_Rev_I (self: t_Rev_I) (return (x: UInt64.t)) =
  {[@stop_split] [@expl:len 'self' type invariant] inv_ref_Rev_I self}
    (! bb0
    [ bb0 = s0 [ s0 = len_I {self.iter} (fun (_x: UInt64.t) -> [ &_ret <- _x ] s1) | s1 = return {_ret} ] ]
    [ & _ret: UInt64.t = Any.any_l () | & self: t_Rev_I = self ])
    [ return (result: UInt64.t) -> {[@stop_split] [@expl:len ensures] postcondition_size_hint'0 () self { f0 = result;
                                                                                                          f1 = Some result }}
      (! return {result}) ]
end
module M_impl_ExactSizeIterator_for_Rev_I__len__refines (* <Rev<I> as common::ExactSizeIterator> *)
  use creusot.int.UInt64
  use seq.Seq
  use creusot.prelude.MutBorrow
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  predicate inv_I (_1: t_I)
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_ref_Rev_I [@inline:trivial] (self: t_Rev_I) = inv_Rev_I self
  
  meta "rewrite_def" predicate invariant_ref_Rev_I
  
  predicate inv_ref_Rev_I [@inline:trivial] (_1: t_Rev_I) = invariant_ref_Rev_I _1
  
  meta "rewrite_def" predicate inv_ref_Rev_I
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate postcondition_size_hint'0 (self: ()) (args: t_Rev_I) (result: tup2_usize_Option_usize) =
    let self'0 = args in postcondition_size_hint () self'0.iter result
  
  meta "rewrite_def" predicate postcondition_size_hint'0
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  function produces_trans_Rev_I (a: t_Rev_I) (ab: Seq.seq t_Item) (b: t_Rev_I) (bc: Seq.seq t_Item) (c: t_Rev_I) : () =
    ()
  
  axiom produces_trans_Rev_I_spec:
    forall a: t_Rev_I, ab: Seq.seq t_Item, b: t_Rev_I, bc: Seq.seq t_Item, c: t_Rev_I. produces_Rev_I a ab b
      -> produces_Rev_I b bc c -> produces_Rev_I a (Seq.(++) ab bc) c
  
  function produces_refl_Rev_I (self: t_Rev_I) : () = ()
  
  axiom produces_refl_Rev_I_spec: forall self: t_Rev_I. produces_Rev_I self (Seq.empty: Seq.seq t_Item) self
  
  let size_hint_Rev_I (self: t_Rev_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint 'self' type invariant] inv_ref_Rev_I self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_size_hint () self.iter result}
      (! return {result}) ]
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines: forall self: t_Rev_I. inv_ref_Rev_I self
      -> inv_ref_Rev_I self
      /\ (forall result: UInt64.t. postcondition_size_hint'0 () self { f0 = result; f1 = Some result }
        -> postcondition_size_hint'0 () self { f0 = result; f1 = Some result })
end
module M_impl_ExactSizeIterator_for_Rev_I__size_hint_exact (* <Rev<I> as common::ExactSizeIterator> *)
  use creusot.int.UInt64
  use seq.Seq
  use creusot.prelude.MutBorrow
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate postcondition_size_hint'0 (self: ()) (args: t_Rev_I) (result: tup2_usize_Option_usize) =
    let self'0 = args in postcondition_size_hint () self'0.iter result
  
  meta "rewrite_def" predicate postcondition_size_hint'0
  
  predicate inv_I (_1: t_I)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  function produces_trans_Rev_I (a: t_Rev_I) (ab: Seq.seq t_Item) (b: t_Rev_I) (bc: Seq.seq t_Item) (c: t_Rev_I) : () =
    ()
  
  axiom produces_trans_Rev_I_spec:
    forall a: t_Rev_I, ab: Seq.seq t_Item, b: t_Rev_I, bc: Seq.seq t_Item, c: t_Rev_I. produces_Rev_I a ab b
      -> produces_Rev_I b bc c -> produces_Rev_I a (Seq.(++) ab bc) c
  
  function produces_refl_Rev_I (self: t_Rev_I) : () = ()
  
  axiom produces_refl_Rev_I_spec: forall self: t_Rev_I. produces_Rev_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_ref_Rev_I [@inline:trivial] (self: t_Rev_I) = inv_Rev_I self
  
  meta "rewrite_def" predicate invariant_ref_Rev_I
  
  predicate inv_ref_Rev_I [@inline:trivial] (_1: t_Rev_I) = invariant_ref_Rev_I _1
  
  meta "rewrite_def" predicate inv_ref_Rev_I
  
  let size_hint_Rev_I (self: t_Rev_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint 'self' type invariant] inv_ref_Rev_I self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_size_hint () self.iter result}
      (! return {result}) ]
  
  function size_hint_exact_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_exact_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> r.f1 = Some (r.f0)
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  constant self : t_Rev_I
  
  constant r : tup2_usize_Option_usize
  
  function size_hint_exact_Rev_I (self: t_Rev_I) (r: tup2_usize_Option_usize) : ()
  
  goal vc_size_hint_exact_Rev_I: postcondition_size_hint'0 () self r
    -> ([@stop_split] [@expl:size_hint_exact requires] postcondition_size_hint () self.iter r)
    /\ (([@stop_split] [@expl:size_hint_exact ensures] r.f1 = Some (r.f0))
    -> ([@stop_split] [@expl:size_hint_exact ensures] r.f1 = Some (r.f0)))
end
module M_impl_ExactSizeIterator_for_Rev_I__size_hint_exact__refines (* <Rev<I> as common::ExactSizeIterator> *)
  use creusot.int.UInt64
  use seq.Seq
  use creusot.prelude.MutBorrow
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate postcondition_size_hint'0 (self: ()) (args: t_Rev_I) (result: tup2_usize_Option_usize) =
    let self'0 = args in postcondition_size_hint () self'0.iter result
  
  meta "rewrite_def" predicate postcondition_size_hint'0
  
  predicate inv_I (_1: t_I)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  function produces_trans_Rev_I (a: t_Rev_I) (ab: Seq.seq t_Item) (b: t_Rev_I) (bc: Seq.seq t_Item) (c: t_Rev_I) : () =
    ()
  
  axiom produces_trans_Rev_I_spec:
    forall a: t_Rev_I, ab: Seq.seq t_Item, b: t_Rev_I, bc: Seq.seq t_Item, c: t_Rev_I. produces_Rev_I a ab b
      -> produces_Rev_I b bc c -> produces_Rev_I a (Seq.(++) ab bc) c
  
  function produces_refl_Rev_I (self: t_Rev_I) : () = ()
  
  axiom produces_refl_Rev_I_spec: forall self: t_Rev_I. produces_Rev_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_ref_Rev_I [@inline:trivial] (self: t_Rev_I) = inv_Rev_I self
  
  meta "rewrite_def" predicate invariant_ref_Rev_I
  
  predicate inv_ref_Rev_I [@inline:trivial] (_1: t_Rev_I) = invariant_ref_Rev_I _1
  
  meta "rewrite_def" predicate inv_ref_Rev_I
  
  let size_hint_Rev_I (self: t_Rev_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint 'self' type invariant] inv_ref_Rev_I self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_size_hint () self.iter result}
      (! return {result}) ]
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines: forall self: t_Rev_I, r: tup2_usize_Option_usize. postcondition_size_hint'0 () self r
      -> postcondition_size_hint'0 () self r /\ (forall result: (). r.f1 = Some (r.f0) -> r.f1 = Some (r.f0))
end
module M_impl_Iterator_for_Rev_I__next (* <Rev<I> as common::Iterator> *)
  use creusot.prelude.MutBorrow
  use creusot.int.UInt64
  use seq.Seq
  use creusot.prelude.Any
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  predicate inv_I (_1: t_I)
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  type t_Option_Item = None'0 | Some'0 t_Item
  
  predicate invariant_refmut_I [@inline:trivial] (self: MutBorrow.t t_I) = inv_I self.current /\ inv_I self.final
  
  meta "rewrite_def" predicate invariant_refmut_I
  
  predicate inv_refmut_I [@inline:trivial] (_1: MutBorrow.t t_I) = invariant_refmut_I _1
  
  meta "rewrite_def" predicate inv_refmut_I
  
  predicate precondition_next_back (self: ()) (args: MutBorrow.t t_I)
  
  axiom precondition_fndef'0:
    forall args: MutBorrow.t t_I [precondition_next_back () args]. (let self = args in inv_refmut_I self)
      -> precondition_next_back () args
  
  predicate inv_Item (_1: t_Item)
  
  predicate inv_Option_Item (_1: t_Option_Item)
  
  axiom inv_axiom [@rewrite]: forall x: t_Option_Item [inv_Option_Item x]. inv_Option_Item x
      = match x with
        | None'0 -> true
        | Some'0 f0'0 -> inv_Item f0'0
        end
  
  predicate postcondition_next_back (self: ()) (args: MutBorrow.t t_I) (result: t_Option_Item)
  
  axiom postcondition_fndef'0:
    forall args: MutBorrow.t t_I, res: t_Option_Item [postcondition_next_back () args res]. postcondition_next_back () args res
      -> (let self = args in inv_Option_Item res
      /\ match res with
        | None'0 -> completed_back_I self
        | Some'0 v -> produces_back_I self.current (Seq.singleton v) self.final
        end)
  
  predicate postcondition_once_next_back (self: ()) (args: MutBorrow.t t_I) (result: t_Option_Item) =
    postcondition_next_back self args result
  
  meta "rewrite_def" predicate postcondition_once_next_back
  
  let next_back_I (self: MutBorrow.t t_I) (return (x: t_Option_Item)) =
  {[@stop_split] [@expl:next_back requires] precondition_next_back () self}
    any
    [ return (result: t_Option_Item) ->
    {[@stop_split] [@expl:next_back ensures] postcondition_once_next_back () self result}
      (! return {result}) ]
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom'0 [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_refmut_Rev_I [@inline:trivial] (self: MutBorrow.t t_Rev_I) =
    inv_Rev_I self.current /\ inv_Rev_I self.final
  
  meta "rewrite_def" predicate invariant_refmut_Rev_I
  
  predicate inv_refmut_Rev_I [@inline:trivial] (_1: MutBorrow.t t_Rev_I) = invariant_refmut_Rev_I _1
  
  meta "rewrite_def" predicate inv_refmut_Rev_I
  
  predicate resolve_refmut_Rev_I [@inline:trivial] (_1: MutBorrow.t t_Rev_I) = _1.final = _1.current
  
  meta "rewrite_def" predicate resolve_refmut_Rev_I
  
  predicate completed_Rev_I (self: MutBorrow.t t_Rev_I) =
    completed_back_I (MutBorrow.borrow_logic self.current.iter self.final.iter (MutBorrow.inherit_id (MutBorrow.get_id self) 0))
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let next_Rev_I (self: MutBorrow.t t_Rev_I) (return (x: t_Option_Item)) =
  {[@stop_split] [@expl:next 'self' type invariant] inv_refmut_Rev_I self}
    (! bb0
    [ bb0 = s0
      [ s0 = MutBorrow.borrow_final <t_I> {self.current.iter} {MutBorrow.inherit_id (MutBorrow.get_id self) 0}
          (fun (_bor: MutBorrow.t t_I) ->
            [ &_4 <- _bor ] -{inv_I _bor.final}-
            [ &self <- { self with current = { iter = _bor.final } } ] s1)
        [ _ck -> (! {[@expl:type invariant] inv_I self.current.iter} any) ]
      | s1 = next_back_I {_4} (fun (_x: t_Option_Item) -> [ &_ret <- _x ] s2)
      | s2 = s3 [ _ck -> (! {[@expl:type invariant] inv_refmut_Rev_I self} any) ]
      | s3 = -{resolve_refmut_Rev_I self}- s4
      | s4 = return {_ret} ] ]
    [ & _ret: t_Option_Item = Any.any_l ()
    | & self: MutBorrow.t t_Rev_I = self
    | & _4: MutBorrow.t t_I = Any.any_l () ])
    [ return (result: t_Option_Item) ->
    {[@stop_split] [@expl:next_Rev_I ensures] ([@stop_split] [@expl:next result type invariant] inv_Option_Item result)
      /\ ([@stop_split] [@expl:next ensures] match result with
        | None'0 -> completed_Rev_I self
        | Some'0 v -> produces_Rev_I self.current (Seq.singleton v) self.final
        end)}
      (! return {result}) ]
end
module M_impl_Iterator_for_Rev_I__next__refines (* <Rev<I> as common::Iterator> *)
  use creusot.prelude.MutBorrow
  use seq.Seq
  use creusot.int.UInt64
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  predicate inv_I (_1: t_I)
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_refmut_Rev_I [@inline:trivial] (self: MutBorrow.t t_Rev_I) =
    inv_Rev_I self.current /\ inv_Rev_I self.final
  
  meta "rewrite_def" predicate invariant_refmut_Rev_I
  
  predicate inv_refmut_Rev_I [@inline:trivial] (_1: MutBorrow.t t_Rev_I) = invariant_refmut_Rev_I _1
  
  meta "rewrite_def" predicate inv_refmut_Rev_I
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  type t_Option_Item = None | Some t_Item
  
  predicate inv_Item (_1: t_Item)
  
  predicate inv_Option_Item (_1: t_Option_Item)
  
  axiom inv_axiom'0 [@rewrite]: forall x: t_Option_Item [inv_Option_Item x]. inv_Option_Item x
      = match x with
        | None -> true
        | Some f0 -> inv_Item f0
        end
  
  type t_Option_usize = None'0 | Some'0 UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some'0 r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None'0 -> true
        end)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some'0 r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None'0 -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate completed_Rev_I (self: MutBorrow.t t_Rev_I) =
    completed_back_I (MutBorrow.borrow_logic self.current.iter self.final.iter (MutBorrow.inherit_id (MutBorrow.get_id self) 0))
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines: forall self: MutBorrow.t t_Rev_I. inv_refmut_Rev_I self
      -> inv_refmut_Rev_I self
      /\ (forall result: t_Option_Item. inv_Option_Item result
          /\ match result with
            | None -> completed_Rev_I self
            | Some v -> produces_Rev_I self.current (Seq.singleton v) self.final
            end
        -> inv_Option_Item result
        /\ match result with
          | None -> completed_Rev_I self
          | Some v -> produces_Rev_I self.current (Seq.singleton v) self.final
          end)
end
module M_impl_Iterator_for_Rev_I__produces_refl (* <Rev<I> as common::Iterator> *)
  use seq.Seq
  use creusot.int.UInt64
  use creusot.prelude.MutBorrow
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate inv_I (_1: t_I)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  constant self : t_Rev_I
  
  function produces_refl_Rev_I (self: t_Rev_I) : ()
  
  goal vc_produces_refl_Rev_I:
    [@stop_split] [@expl:produces_refl ensures] produces_Rev_I self (Seq.empty: Seq.seq t_Item) self
end
module M_impl_Iterator_for_Rev_I__produces_refl__refines (* <Rev<I> as common::Iterator> *)
  use seq.Seq
  use creusot.int.UInt64
  use creusot.prelude.MutBorrow
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate inv_I (_1: t_I)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines: forall self: t_Rev_I. forall result: (). produces_Rev_I self (Seq.empty: Seq.seq t_Item) self
        -> produces_Rev_I self (Seq.empty: Seq.seq t_Item) self
end
module M_impl_Iterator_for_Rev_I__produces_trans (* <Rev<I> as common::Iterator> *)
  use seq.Seq
  use creusot.int.UInt64
  use creusot.prelude.MutBorrow
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate inv_I (_1: t_I)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  constant a : t_Rev_I
  
  constant ab : Seq.seq t_Item
  
  constant b : t_Rev_I
  
  constant bc : Seq.seq t_Item
  
  constant c : t_Rev_I
  
  function produces_trans_Rev_I (a: t_Rev_I) (ab: Seq.seq t_Item) (b: t_Rev_I) (bc: Seq.seq t_Item) (c: t_Rev_I) : ()
  
  goal vc_produces_trans_Rev_I: produces_Rev_I a ab b
    -> produces_Rev_I b bc c -> ([@stop_split] [@expl:produces_trans ensures] produces_Rev_I a (Seq.(++) ab bc) c)
end
module M_impl_Iterator_for_Rev_I__produces_trans__refines (* <Rev<I> as common::Iterator> *)
  use seq.Seq
  use creusot.int.UInt64
  use creusot.prelude.MutBorrow
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate inv_I (_1: t_I)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines: forall a: t_Rev_I, ab: Seq.seq t_Item, b: t_Rev_I, bc: Seq.seq t_Item, c: t_Rev_I. produces_Rev_I a ab b
      -> produces_Rev_I b bc c
      -> produces_Rev_I a ab b
      /\ produces_Rev_I b bc c
      /\ (forall result: (). produces_Rev_I a (Seq.(++) ab bc) c -> produces_Rev_I a (Seq.(++) ab bc) c)
end
module M_impl_Iterator_for_Rev_I__size_hint (* <Rev<I> as common::Iterator> *)
  use seq.Seq
  use creusot.int.UInt64
  use creusot.prelude.MutBorrow
  use creusot.prelude.Any
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  predicate inv_I (_1: t_I)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_ref_Rev_I [@inline:trivial] (self: t_Rev_I) = inv_Rev_I self
  
  meta "rewrite_def" predicate invariant_ref_Rev_I
  
  predicate inv_ref_Rev_I [@inline:trivial] (_1: t_Rev_I) = invariant_ref_Rev_I _1
  
  meta "rewrite_def" predicate inv_ref_Rev_I
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let size_hint_Rev_I (self: t_Rev_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint 'self' type invariant] inv_ref_Rev_I self}
    (! bb0
    [ bb0 = s0
      [ s0 = size_hint_I {self.iter} (fun (_x: tup2_usize_Option_usize) -> [ &_ret <- _x ] s1) | s1 = return {_ret} ] ]
    [ & _ret: tup2_usize_Option_usize = Any.any_l () | & self: t_Rev_I = self ])
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_size_hint () self.iter result}
      (! return {result}) ]
end
module M_impl_Iterator_for_Rev_I__size_hint__refines (* <Rev<I> as common::Iterator> *)
  use creusot.int.UInt64
  use seq.Seq
  use creusot.prelude.MutBorrow
  use int.Int
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  predicate inv_I (_1: t_I)
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  predicate invariant_ref_Rev_I [@inline:trivial] (self: t_Rev_I) = inv_Rev_I self
  
  meta "rewrite_def" predicate invariant_ref_Rev_I
  
  predicate inv_ref_Rev_I [@inline:trivial] (_1: t_Rev_I) = invariant_ref_Rev_I _1
  
  meta "rewrite_def" predicate inv_ref_Rev_I
  
  type t_Option_usize = None | Some UInt64.t
  
  type tup2_usize_Option_usize = { f0: UInt64.t; f1: t_Option_usize }
  
  type t_Item
  
  predicate produces_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function produces_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_trans_I_spec: forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_I a ab b
      -> produces_I b bc c -> produces_I a (Seq.(++) ab bc) c
  
  function produces_refl_I (self: t_I) : ()
  
  axiom produces_refl_I_spec: forall self: t_I. produces_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate completed_I (self: MutBorrow.t t_I)
  
  predicate postcondition_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize)
  
  axiom postcondition_fndef:
    forall args: t_I, res: tup2_usize_Option_usize [postcondition_size_hint () args res]. postcondition_size_hint () args res
      -> (let self = args in (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_I self s i.current /\ completed_I i
          -> UInt64.t'int res.f0 <= Seq.length s)
      /\ match res.f1 with
        | Some r -> forall s: Seq.seq t_Item, i: t_I. produces_I self s i -> Seq.length s <= UInt64.t'int r
        | None -> true
        end)
  
  predicate invariant_ref_I [@inline:trivial] (self: t_I) = inv_I self
  
  meta "rewrite_def" predicate invariant_ref_I
  
  predicate inv_ref_I [@inline:trivial] (_1: t_I) = invariant_ref_I _1
  
  meta "rewrite_def" predicate inv_ref_I
  
  predicate precondition_size_hint (self: ()) (args: t_I)
  
  axiom precondition_fndef: forall args: t_I [precondition_size_hint () args]. (let self = args in inv_ref_I self)
      -> precondition_size_hint () args
  
  predicate postcondition_once_size_hint (self: ()) (args: t_I) (result: tup2_usize_Option_usize) =
    postcondition_size_hint self args result
  
  meta "rewrite_def" predicate postcondition_once_size_hint
  
  let size_hint_I (self: t_I) (return (x: tup2_usize_Option_usize)) =
  {[@stop_split] [@expl:size_hint requires] precondition_size_hint () self}
    any
    [ return (result: tup2_usize_Option_usize) ->
    {[@stop_split] [@expl:size_hint ensures] postcondition_once_size_hint () self result}
      (! return {result}) ]
  
  predicate completed_back_I (self: MutBorrow.t t_I)
  
  predicate produces_back_I (self: t_I) (visited: Seq.seq t_Item) (o: t_I)
  
  function size_hint_back_spec_I (self: t_I) (r: tup2_usize_Option_usize) : ()
  
  axiom size_hint_back_spec_I_spec: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_I. produces_back_I self s i.current /\ completed_back_I i
        -> UInt64.t'int r.f0 <= Seq.length s)
  
  axiom size_hint_back_spec_I_spec'0: forall self: t_I, r: tup2_usize_Option_usize. postcondition_size_hint () self r
      -> match r.f1 with
        | Some r'0 -> forall s: Seq.seq t_Item, i: t_I. produces_back_I self s i -> Seq.length s <= UInt64.t'int r'0
        | None -> true
        end
  
  function produces_back_trans_I (a: t_I) (ab: Seq.seq t_Item) (b: t_I) (bc: Seq.seq t_Item) (c: t_I) : ()
  
  axiom produces_back_trans_I_spec:
    forall a: t_I, ab: Seq.seq t_Item, b: t_I, bc: Seq.seq t_Item, c: t_I. produces_back_I a ab b
      -> produces_back_I b bc c -> produces_back_I a (Seq.(++) ab bc) c
  
  function produces_back_refl_I (self: t_I) : ()
  
  axiom produces_back_refl_I_spec: forall self: t_I. produces_back_I self (Seq.empty: Seq.seq t_Item) self
  
  predicate produces_Rev_I (self: t_Rev_I) (visited: Seq.seq t_Item) (o: t_Rev_I) =
    produces_back_I self.iter visited o.iter
  
  predicate completed_Rev_I (self: MutBorrow.t t_Rev_I) =
    completed_back_I (MutBorrow.borrow_logic self.current.iter self.final.iter (MutBorrow.inherit_id (MutBorrow.get_id self) 0))
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines: forall self: t_Rev_I. inv_ref_Rev_I self
      -> inv_ref_Rev_I self
      /\ (forall result: tup2_usize_Option_usize. postcondition_size_hint () self.iter result
        -> (forall s: Seq.seq t_Item, i: MutBorrow.t t_Rev_I. produces_Rev_I self s i.current /\ completed_Rev_I i
            -> UInt64.t'int result.f0 <= Seq.length s)
        /\ match result.f1 with
          | Some r -> forall s: Seq.seq t_Item, i: t_Rev_I. produces_Rev_I self s i -> Seq.length s <= UInt64.t'int r
          | None -> true
          end)
end
module M_rev
  use creusot.prelude.Any
  
  type t_I
  
  type t_Rev_I = { iter: t_I }
  
  predicate inv_I (_1: t_I)
  
  predicate inv_Rev_I (_1: t_Rev_I)
  
  axiom inv_axiom [@rewrite]: forall x: t_Rev_I [inv_Rev_I x]. inv_Rev_I x = inv_I x.iter
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let rev_I (iter'0: t_I) (return (x: t_Rev_I)) = {[@stop_split] [@expl:rev 'iter' type invariant] inv_I iter'0}
    (! bb0
    [ bb0 = s0 [ s0 = [ &_ret <- { iter = iter'0 } ] s1 | s1 = return {_ret} ] ]
    [ & _ret: t_Rev_I = Any.any_l () | & iter'0: t_I = iter'0 ])
    [ return (result: t_Rev_I) ->
    {[@stop_split] [@expl:rev_I ensures] ([@stop_split] [@expl:rev result type invariant] inv_Rev_I result)
      /\ ([@stop_split] [@expl:rev ensures] result = { iter = iter'0 })}
      (! return {result}) ]
end
