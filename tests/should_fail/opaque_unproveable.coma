module M_test
  use creusot.prelude.Any
  use creusot.prelude.Mode
  
  predicate opaque
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let test (mode: Mode.t) (return (x: ())) = (! bb0
    [ bb0 = s0 [ s0 = {[@expl:assertion] opaque} s1 | s1 = return {_ret} ] ] [ & _ret: () = Any.any_l () ])
    [ return (result: ()) -> (! return {result}) ]
end
