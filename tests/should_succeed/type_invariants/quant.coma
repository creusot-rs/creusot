module M_quant__forall
  predicate invariant' (self: ()) = true
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  function forall' : ()
  
  goal vc_forall: [@expl:forall ensures] forall x: (). invariant' x
end
module M_quant__exists
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  function exists' : ()
  
  goal vc_exists: [@expl:exists ensures] exists _x: (). true
end
