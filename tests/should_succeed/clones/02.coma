module M_02__program
  use creusot.prelude.Any
  
  predicate simple = true
  
  predicate uses_simple = simple
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let rec program (return' (x: ())) = {[@expl:program requires] uses_simple}
    (! bb0
    [ bb0 = return''0 {_0} ] [ & _0: () = Any.any_l () ])
    [ return''0 (result: ()) -> {[@expl:program ensures] simple} (! return' {result}) ]
end
