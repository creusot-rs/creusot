module M_calls_f
  use creusot.prelude.Mode
  use creusot.prelude.Any
  
  let f_i32 (mode: Mode.t) (return (x: ())) = any [ return (result: ()) -> (! return {result}) ]
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let calls_f (mode: Mode.t) (return (x: ())) = (! bb0
    [ bb0 = s0 [ s0 = f_i32 {mode} (fun (_x: ()) -> [ &_ret <- _x ] s1) | s1 = return {_ret} ] ]
    [ & _ret: () = Any.any_l () ]) [ return (result: ()) -> (! return {result}) ]
end
module M_clone_id
  use creusot.prelude.Mode
  use creusot.prelude.Any
  
  type t_Id
  
  let clone_Id (mode: Mode.t) (self: t_Id) (return (x: t_Id)) = any
    [ return (result: t_Id) -> {[@stop_split] [@expl:clone ensures] result = self} (! return {result}) ]
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let clone_id (mode: Mode.t) (i: t_Id) (return (x: ())) = (! bb0
    [ bb0 = s0 [ s0 = clone_Id {mode} {i} (fun (_x: t_Id) -> [ &_2 <- _x ] s1) | s1 = return {_ret} ] ]
    [ & _ret: () = Any.any_l () | & i: t_Id = i | & _2: t_Id = Any.any_l () ])
    [ return (result: ()) -> (! return {result}) ]
end
module M_impl_Foo_for_i32__f (* <i32 as Foo> *)
  use creusot.prelude.Any
  use creusot.prelude.Mode
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let f_i32 (mode: Mode.t) (return (x: ())) = (! bb0
    [ bb0 = return {_ret} ] [ & _ret: () = Any.any_l () ]) [ return (result: ()) -> (! return {result}) ]
end
module M_result
  use creusot.prelude.Any
  use creusot.prelude.Mode
  
  constant g_i32: int = 1
  
  constant calls_g: int = g_i32
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let result (mode: Mode.t) (return (x: ())) = (! bb0
    [ bb0 = s0 [ s0 = {[@expl:assertion] calls_g = 1} s1 | s1 = return {_ret} ] ] [ & _ret: () = Any.any_l () ])
    [ return (result: ()) -> (! return {result}) ]
end
