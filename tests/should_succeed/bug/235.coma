module M_f
  use mach.int.Int
  use creusot.prelude.Any
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let rec f (return (x: ())) = (! bb0
    [ bb0 = bb0
      [ bb0 = {[@expl:loop invariant] 0 <= 1} (! s0) [ s0 = any [ br0 -> {false} (! bb4) | br1 -> {true} (! bb0) ] ] ]
    | bb4 = return {_ret} ] [ & _ret: () = Any.any_l () ]) [ return (result: ()) -> (! return {result}) ]
end
