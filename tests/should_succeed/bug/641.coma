module M_test_maintains
  use creusot.prelude.Any
  
  predicate test = true
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let rec test_maintains (return (x: ())) = {[@expl:test_maintains requires] test}
    (! bb0
    [ bb0 = return {_ret} ] [ & _ret: () = Any.any_l () ])
    [ return (result: ()) -> {[@expl:test_maintains ensures] test} (! return {result}) ]
end
