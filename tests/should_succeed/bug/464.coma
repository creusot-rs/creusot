module M_impl_Trait_for_Struct__invariant (* <Struct as Trait> *)
  predicate invariant_AssocStruct (self: ()) = true
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  constant self : ()
  
  constant assoc : ()
  
  predicate invariant_Struct (self: ()) (assoc: ())
  
  goal vc_invariant_Struct: [@expl:invariant ensures] true -> invariant_AssocStruct assoc
end
module M_impl_Trait_for_Struct__invariant__refines (* <Struct as Trait> *)
  predicate invariant_AssocStruct (self: ()) = true
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines: forall self: (). forall assoc: (). forall result: bool. result -> invariant_AssocStruct assoc
          -> result -> invariant_AssocStruct assoc
end
