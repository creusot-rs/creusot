module M_h
  use creusot.prelude.Any
  
  type t_Option_bool = None | Some bool
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let rec h (return (x: t_Option_bool)) = (! bb0
    [ bb0 = s0 [ s0 = [ &_ret <- Some true ] s1 | s1 = return {_ret} ] ] [ & _ret: t_Option_bool = Any.any_l () ])
    [ return (result: t_Option_bool) -> {[@expl:h ensures] match result with
        | Some (True) -> true
        | _ -> false
        end}
      (! return {result}) ]
end
