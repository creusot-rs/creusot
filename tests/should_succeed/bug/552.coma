module M_impl_MachineTrait_for_Machine__step (* <Machine as MachineTrait> *)
  use creusot.prelude.MutBorrow
  use creusot.prelude.Any
  
  predicate resolve_ref_Machine [@inline:trivial] (_1: MutBorrow.t ()) = _1.final = _1.current
  
  meta "rewrite_def" predicate resolve_ref_Machine
  
  predicate invariants_Machine (self: ()) = true
  
  let rec transition (self: ()) (return (x: ())) = {[@expl:transition requires] invariants_Machine self}
    any [ return (result: ()) -> (! return {result}) ]
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let rec step_Machine (self: MutBorrow.t ()) (return (x: bool)) =
    {[@expl:step requires] invariants_Machine self.current}
    (! bb0
    [ bb0 = s0
      [ s0 = -{resolve_ref_Machine self}- s1
      | s1 = transition {self.current} (fun (_ret: ()) -> [ &_4 <- _ret ] s2)
      | s2 = bb1 ]
    | bb1 = s0 [ s0 = [ &_0 <- false ] s1 | s1 = return {_0} ] ]
    [ & _0: bool = Any.any_l () | & self: MutBorrow.t () = self | & _4: () = Any.any_l () ])
    [ return (result: bool) -> {[@expl:step ensures] invariants_Machine self.final} (! return {result}) ]
end
module M_impl_Machine__transition (* Machine *)
  use creusot.prelude.Any
  
  predicate invariants_Machine (self: ()) = true
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let rec transition (self: ()) (return (x: ())) = {[@expl:transition requires] invariants_Machine self}
    (! bb0
    [ bb0 = s0 [ s0 = [ &_0 <- () ] s1 | s1 = return {_0} ] ] [ & _0: () = Any.any_l () ])
    [ return (result: ()) -> (! return {result}) ]
end
module M_impl_MachineTrait_for_Machine__step__refines (* <Machine as MachineTrait> *)
  use creusot.prelude.MutBorrow
  
  predicate invariants_Machine (self: ()) = true
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  goal refines: forall self: MutBorrow.t (). invariants_Machine self.current
      -> invariants_Machine self.current
      /\ (forall result: bool. invariants_Machine self.final -> invariants_Machine self.final)
end
