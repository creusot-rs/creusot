module M_nonreturning__f
  predicate invariant' [@inline:trivial] (self: ()) = false
  
  meta "rewrite_def" predicate invariant'
  
  predicate inv (_1: ())
  
  axiom inv_axiom [@rewrite]: forall x: () [inv x]. inv x = invariant' x
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let rec f (return (x: ())) = (! bb0
    [ bb0 = bb1 | bb1 = bb1 [ bb1 = (! bb1) ] ])
    [ return (result: ()) -> {[@expl:f result type invariant] inv result} (! return {result}) ]
end
module M_nonreturning__g
  use creusot.prelude.Any
  
  predicate invariant' [@inline:trivial] (self: ()) = false
  
  meta "rewrite_def" predicate invariant'
  
  predicate inv (_1: ())
  
  axiom inv_axiom [@rewrite]: forall x: () [inv x]. inv x = invariant' x
  
  let rec f (return (x: ())) = any [ return' (result: ()) -> {inv result} (! return {result}) ]
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  let rec g (b: bool) (return (x: ())) = (! bb0
    [ bb0 = any [ br0 -> {b = false} (! bb2) | br1 -> {b} (! bb1) ]
    | bb1 = s0 [ s0 = f (fun (_ret: ()) -> [ &_5 <- _ret ] s1) | s1 = {false} any ]
    | bb2 = return {_0} ] [ & _0: () = Any.any_l () | & b: bool = b | & _5: () = Any.any_l () ])
    [ return (result: ()) -> {[@expl:g ensures] not b} (! return {result}) ]
end
