module M_unsized_quant__l
  use creusot.slice.Slice64
  use creusot.int.Int32
  use seq.Seq
  use map.Map
  use mach.int.Int
  
  function index_logic [@inline:trivial] (self: Map.map (Slice64.slice Int32.t) int) (a: Slice64.slice Int32.t) : int =
    Map.get self a
  
  meta "rewrite_def" function index_logic
  
  predicate f =
    let len = fun (x: Slice64.slice Int32.t) -> Seq.length (Slice64.view x) in forall x: Slice64.slice Int32.t, y: Slice64.slice Int32.t. index_logic len x
        + index_logic len y
      >= 0
  
  meta "compute_max_steps" 1000000
  
  meta "select_lsinst" "all"
  
  function l : ()
  
  goal vc_l: [@expl:l ensures] f
end
