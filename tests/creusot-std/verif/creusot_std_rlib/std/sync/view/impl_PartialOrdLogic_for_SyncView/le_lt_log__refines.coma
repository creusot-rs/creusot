(* <std::sync::view::SyncView as logic::ord::PartialOrdLogic> *)

type namespace_other

type t_Namespace = Other namespace_other

type t_SyncView

predicate lt_log_SyncView (self: t_SyncView) (_2: t_SyncView)

predicate le_log_SyncView [@inline:trivial] (self: t_SyncView) (other: t_SyncView) =
  lt_log_SyncView self other \/ self = other

meta "rewrite_def" predicate le_log_SyncView

meta "compute_max_steps" 1000000

meta "select_lsinst" "all"

goal refines: forall self: t_SyncView, other: t_SyncView. forall result: (). le_log_SyncView self other
        = (lt_log_SyncView self other \/ self = other)
      -> le_log_SyncView self other = (lt_log_SyncView self other \/ self = other)
