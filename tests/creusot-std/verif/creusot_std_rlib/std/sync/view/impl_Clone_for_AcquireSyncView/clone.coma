(* <std::sync::view::AcquireSyncView as std::clone::Clone> *)

type namespace_other

type t_Namespace = Other namespace_other

use creusot.prelude.Any

type t_AcquireSyncView

meta "compute_max_steps" 1000000

meta "select_lsinst" "all"

let clone_AcquireSyncView (self: t_AcquireSyncView) (return (x: t_AcquireSyncView)) = (! bb0
  [ bb0 = s0 [ s0 = [ &_ret <- self ] s1 | s1 = return {_ret} ] ]
  [ & _ret: t_AcquireSyncView = Any.any_l () | & self: t_AcquireSyncView = self ])
  [ return (result: t_AcquireSyncView) -> {[@stop_split] [@expl:clone ensures] result = self} (! return {result}) ]
